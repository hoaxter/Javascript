let start; //undefined  // we only defined the variable not its value so it will point undefined
let start1 = undefined;
console.log(start); // undefined was naturally given by javascript engine
console.log(start1); // we can give undefined value by ourselves but we don't do it instead we use null

let start3 = null;
console.log(start3);

